// String methods

let str = "hello";

// str methods
const strArr = str.split(""); // returns new arr
console.log(strArr);
console.log(str);

// Getting the char code, and generating char code. a-z is
console.log(String.fromCharCode(97, 122)); // can get a strong of all
console.log(str.charCodeAt(0));
